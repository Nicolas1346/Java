package com.mycompany.dezeito;
import java.util.Scanner;
public class Dezeito {

    public static void main(String[] args) {
        Scanner sc = new Scanner (System.in);
        
        System.out.print("Digite a primeiro numero: ");
        int a = sc.nextInt();
        
        System.out.print("Digite o segundo numero: ");
        int b = sc.nextInt();
        
        System.out.print("Digite o terceiro numero: ");
        int c = sc.nextInt();
        
        System.out.print("Digite o quarto numero: ");
        int d = sc.nextInt();
        
        System.out.print("Digite o quinto numero: ");
        int e = sc.nextInt();
        
        int maior = a;
        
            if (b > maior) maior = b;    
            if (c > maior) maior = c;
            if (d > maior) maior = d;
            if (e > maior) maior = e;

        System.out.println("O maior valor e: "  + maior);

        sc.close();
    }
}

